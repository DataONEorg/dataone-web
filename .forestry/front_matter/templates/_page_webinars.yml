---
label: "\U0001F4C4Webinars Overview Page"
hide_body: true
display_field: title
fields:
- type: list
  name: webinar_tags
  label: Webinar Tags
  description: Data lifecycle stages used to categorize webinars
- type: text
  label: Title
  name: title
  description: The title will display at the top of the page, in the navigation (if
    a name is not set in the menu), in search results, the browser tab, and bookmarked
    page
- type: text
  label: Description
  name: description
  description: A description of the page. This description will appear in search results.
- name: header
  type: field_group
  label: Webinar Header
  fields:
  - name: image
    type: field_group
    config: {}
    fields:
    label: Default background image
    description: The image that will be displayed as a background image if there isn't
      one for the upcoming webinar
    - name: image
      type: include
      config: {}
      template: image
      label: Image
      description: The image that will be displayed as a background image if there
        isn't one for the upcoming webinar
  - type: textarea
    label: Page headline
    name: title
    description: The title or headline to display at the top of the page in large
      text
    config:
      wysiwyg: true
      schema:
        format: markdown
  - type: textarea
    label: Page introduction
    name: intro
    description: Brief text that introduces the page
    config:
      wysiwyg: true
      schema:
        format: markdown
  - name: pill
    type: field_group
    config: {}
    label: Header Pill
    description: An optional pill that will appear above the page title
    fields:
    - name: pill
      type: include
      config: {}
      template: pill
      label: Pill
- type: field_group_list
  name: page_sections
  label: Page sections
  fields:
  - type: include
    template: section
    name: section-block
    label: Section block
    description: Modular sections to organize content
  description: The different building blocks that make up the page sections
  config:
    labelField: title
pages:
- content/webinars/_index.md
