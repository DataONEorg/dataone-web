---
label: Job summary
hide_body: true
display_field: job
fields:
- type: text
  name: template
  label: Template
  hidden: true
  default: job-summary
- type: boolean
  name: auto_select
  label: Select most recently added job
  default: false
  description: If true, the most recently added position will be shown.
- name: job
  type: list
  config:
    use_select: true
    min: '1'
    max: '1'
    source:
      type: pages
      section: jobs
  label: Jobs
  description: Which job summary should be displayed?
  showOnly:
    field: auto_select
    value: false
