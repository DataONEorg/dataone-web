---
label: Timeline
hide_body: true
fields:
- type: text
  name: template
  label: Template
  hidden: true
  default: timeline
- name: event
  type: field_group_list
  label: Event
  description: A single event in a timeline
  fields:
  - name: event
    type: include
    config: {}
    template: card
    label: Event
    description: A single event in a timeline
  config:
    labelField: title
