---
label: Metrics
hide_body: true
fields:
- type: text
  name: template
  label: Template
  hidden: true
  default: metrics
- type: select
  label: Metrics style
  name: type
  description: The style to use for the metrics
  config:
    source:
      type: simple
    options:
    - default
    - accent
  default: default
